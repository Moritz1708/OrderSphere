@page "/checkout/success"
@attribute [Authorize]
@inject IOrderingClient Ordering
@inject NavigationManager Navigation
@inject IStringLocalizer<AppStrings> L

<PageTitle>@L["CheckoutSuccess.PageTitle"]</PageTitle>

<section style="padding:var(--os-section-py-md) 0;">
    <MudContainer MaxWidth="MaxWidth.Small" Class="text-center">

        <IconBadge CssClass="avatar-initials" Style="margin:0 auto 24px;">
            <MudIcon Icon="@Icons.Material.Outlined.CheckCircle" Style="font-size:48px;color:var(--mud-palette-primary-text);" />
        </IconBadge>

        <MudText Typo="Typo.h3" Style="font-weight:700;">@L["CheckoutSuccess.Title"]</MudText>
        <MudText Typo="Typo.body1" Class="text-muted mt-3">
            @L["CheckoutSuccess.Body"]
        </MudText>

        @if (_totalAmount.HasValue)
        {
            <MudText Typo="Typo.h5" Class="mt-4" Style="font-weight:600;">
                @L["CheckoutSuccess.Total", Formatting.Currency(_totalAmount.Value)]
            </MudText>
        }

        @if (_order is not null)
        {
            <MudPaper Elevation="0" Class="surface-card pa-6 mt-6 text-left">
                <MudText Typo="Typo.caption" Class="text-muted">@L["CheckoutSuccess.OrderNumber"]</MudText>
                <MudText Typo="Typo.subtitle1" Class="text-monospace" Style="font-weight:600;">
                    #@_order.Id.ToString("N")[..8].ToUpperInvariant()
                </MudText>
            </MudPaper>
        }

        <MudStack Row="true" Justify="Justify.Center" Spacing="3" Class="mt-8">
            <MudButton Href="/account/orders" Variant="Variant.Filled"
                       Color="Color.Primary" Size="Size.Large" Class="btn-pill">
                @L["Account.MyOrders"]
            </MudButton>
            <MudButton Href="/shop" Variant="Variant.Outlined"
                       Color="Color.Primary" Size="Size.Large" Class="btn-pill">
                @L["CheckoutSuccess.ContinueShopping"]
            </MudButton>
        </MudStack>

    </MudContainer>
</section>

@code {
    [SupplyParameterFromQuery] private Guid? CorrelationId { get; set; }
    [SupplyParameterFromQuery] private string? Total { get; set; }

    private OrderDto? _order;
    private decimal? _totalAmount;

    protected override async Task OnInitializedAsync()
    {
        if (decimal.TryParse(Total, System.Globalization.NumberStyles.Any,
            System.Globalization.CultureInfo.InvariantCulture, out var parsed))
        {
            _totalAmount = parsed;
        }

        if (CorrelationId is null) return;

        // Poll for the order (Service Bus processing may take a moment).
        // Ownership is enforced server-side via the JWT token.
        for (var i = 0; i < 6; i++)
        {
            _order = await Ordering.GetOrderByCorrelationIdAsync(CorrelationId.Value);
            if (_order is not null) break;
            await Task.Delay(2000);
        }
    }
}
