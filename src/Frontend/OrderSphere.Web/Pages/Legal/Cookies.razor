@page "/cookies"
@inject IStringLocalizer<AppStrings> L

<PageTitle>@L["Cookies.PageTitle"]</PageTitle>

<ArticleLayout Eyebrow="@L["Footer.Legal"]"
               Title="@L["Cookies.Title"]"
               Lede="@L["Cookies.Lede"]"
               Notice="@L["Article.PlaceholderNotice"]"
               Sections="_sections" />

@code {
    private IReadOnlyList<ArticleSection> _sections = [];

    protected override void OnInitialized() => _sections =
    [
        Section("what", 1),
        Section("which", 2),
        Section("manage", 3),
    ];

    private ArticleSection Section(string id, int n) =>
        new(id, L[$"Cookies.S{n}.Title"], new MarkupString(L[$"Cookies.S{n}.Body"]));
}
